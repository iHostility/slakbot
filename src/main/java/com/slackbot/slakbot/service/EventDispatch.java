package com.slackbot.slakbot.service;

public class EventDispatch {
//    public static Boolean isURLVerification(Event event) throws IOException {
//        JsonNode node = JsonProcessing.parse(event.getIncomingEvent());
//        EventPOJO pojo = JsonProcessing.fromJson(node, EventPOJO.class);
//        return (pojo.getType().equalsIgnoreCase("url_verification"));
//    }
//
//    public static URLVerify eventToUrlVerify(Event event) throws IOException {
//        JsonNode node = JsonProcessing.parse(event.getIncomingEvent());
//        return new URLVerify(
//                node.get("token").asText(),
//                node.get("challenge").asText(),
//                node.get("type").asText());
//    }
//
//    public static Boolean isEventCallback(Event event) throws IOException {
//        JsonNode node = JsonProcessing.parse(event.getIncomingEvent());
//        EventPOJO pojo = JsonProcessing.fromJson(node, EventPOJO.class);
//        return (pojo.getType().equalsIgnoreCase("event_callback"));
//    }
}
